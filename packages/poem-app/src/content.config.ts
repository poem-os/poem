/**
 * Astro Content Collections Configuration
 * POEM doesn't use content collections - this file exists to prevent
 * the "[WARN] [content] Content config not loaded" warning.
 */
import { defineCollection } from 'astro:content';

// No collections defined - POEM uses its own content management
export const collections = {};
